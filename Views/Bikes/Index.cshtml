@model IEnumerable<Bike155.Models.Bike>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BakeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BakeDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ruta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BakeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BakeDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ruta.RutaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BikeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BikeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BikeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
